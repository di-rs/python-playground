[project]
name = "neo5j"
version = "0.1.0"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "neo4j>=6.3.0",
    "pydantic-settings>=2.15.0",
]

[tool.fastapi]
entrypoint = "neo5j.main:app"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
