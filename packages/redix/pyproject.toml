[project]
name = "redix"
version = "0.1.0"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "pydantic-settings>=2.15.0",
    "redis[hiredis]>=8.1.0",
]

[tool.fastapi]
entrypoint = "redix.main:app"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
